please install these first, before run the code

pip install happytransformer 
pip install textblob
pip install sentencepiece
pip install transformers

#To use GRADIO
pip install wrapt
pip install anyio
pip install gradio
import gradio as gr

#To use Flask to put code to localhost
pip install flask

thank you ma'am